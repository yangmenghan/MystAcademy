package com.mystacademy.android.utils

class Utils/*

     // Cheatsheets

     - Simple 2 frames animation !
        val constraintOld = ConstraintSet()
        constraintOld.clone(root_cl)
        val constraintNew = ConstraintSet()
        constraintNew.clone(this, R.layout.activity_splash_login)

        TransitionManager.beginDelayedTransition(root_cl)
        constraintNew.applyTo(root_cl)

        */
